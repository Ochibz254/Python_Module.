# This input function asks the user for their name and stores it in a variable called name.
name = input('Name: ')

# This input function asks the user for their age and stores it in a variable called age.
age = input('Age: ')

# This input function asks the user for their location and stores it in a variable called location.
location = input('Location: ')

# This prints a customized message using all the variables.
print(f'Hello {name}, you are {age} years old and live in {location}.')
